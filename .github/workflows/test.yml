name: Test

on:
  push:
    branches:
      - master
  pull_request:

permissions:
  contents: read

concurrency:
  group: test-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
      PERL_MM_USE_DEFAULT: 1
      NONINTERACTIVE_TESTING: 1
      PERL_EXTUTILS_AUTOINSTALL: --defaultdeps
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v5.2.2

      - name: Setup Perl
        uses: shogo82148/actions-setup-perl@9c1eca9952ccc07f9ca4a2097b63df93d9d138e9  # v1.32.0
        with:
          perl-version: '5.44'

      - name: Install project dependencies
        run: cpanm --installdeps --notest -L local .

      - name: Install the audit tool outside the scanned root
        run: cpanm --notest -L audit-local CPAN::Audit

      - name: Audit isolated Perl dependencies
        env:
          PERL5LIB: ${{ github.workspace }}/audit-local/lib/perl5
          PATH: ${{ github.workspace }}/audit-local/bin:${{ env.PATH }}
        run: script/cpan-audit-project local/lib/perl5

      - name: Install test tools and export project environment
        run: |
          cpanm --notest -L local Devel::Cover
          echo "PERL5LIB=$(perl -Ilocal/lib/perl5 -Mlocal::lib=local -e 'print $ENV{PERL5LIB}')" >> "$GITHUB_ENV"
          echo "$GITHUB_WORKSPACE/local/bin" >> "$GITHUB_PATH"

      - name: Run tests
        run: |
          prove -lr t

      - name: Verify lib coverage
        run: |
          cover -delete
          HARNESS_PERL_SWITCHES=-MDevel::Cover prove -lr t
          report="$(cover -report text -select_re '^lib/' -coverage statement -coverage subroutine)"
          printf '%s\n' "$report"
          printf '%s\n' "$report" | grep -E '^Total[[:space:]]+100\.0[[:space:]]+100\.0[[:space:]]+100\.0$'
